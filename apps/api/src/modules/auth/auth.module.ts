import { Module } from '@nestjs/common';

/**
 * Autenticación (Fase 1): registro, login, refresh, verificación de email,
 * guard JWT compartido con el frontend (Auth.js v5).
 */
@Module({})
export class AuthModule {}
